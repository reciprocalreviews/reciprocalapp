/** Whether the platform is in beta. When true, a site-wide beta banner is shown
 * on every page (locally, in staging, and in production). Set to false when the
 * beta period ends. */
export const BETA = true;
